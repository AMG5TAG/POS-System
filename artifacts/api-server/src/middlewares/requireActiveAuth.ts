// requireActiveAuth is now equivalent to requireAuth — both verify the
// merchant account is active. Re-exported here for backwards compatibility.
export { requireAuth as requireActiveAuth } from "./requireAuth";
