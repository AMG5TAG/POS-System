kind = "web"
previewPath = "/"
title = "KoaPOS"
version = "1.0.0"
id = "artifacts/koapos"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19141

[services.development]
run = "pnpm --filter @workspace/koapos run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/koapos", "run", "build" ]
publicDir = "artifacts/koapos/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19141"
BASE_PATH = "/"
